self-hosted-runner:
  labels:
    # Hosted (depot) runners for PR checks, the integration test, and the legacy
    # k6 build-docker workflow.
    - depot-ubuntu-24.04
    - depot-ubuntu-24.04-4
    - depot-ubuntu-24.04-8
    - depot-ubuntu-24.04-16
